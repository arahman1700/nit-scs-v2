---
phase: 06-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/pages/warehouse/YardDashboard.tsx
  - packages/frontend/src/pages/warehouse/yard/YardKpiCards.tsx
  - packages/frontend/src/pages/warehouse/yard/ScheduleModal.tsx
  - packages/frontend/src/pages/warehouse/yard/CheckInTruckModal.tsx
  - packages/frontend/src/pages/warehouse/yard/YardDockGrid.tsx
  - packages/frontend/src/pages/warehouse/yard/YardAppointmentsTable.tsx
  - packages/frontend/src/pages/warehouse/yard/YardTrucksTable.tsx
  - packages/frontend/src/pages/warehouse/yard/YardDockChart.tsx
  - packages/frontend/src/pages/admin/NotificationRulesPage.tsx
  - packages/frontend/src/pages/admin/notifications/EmailTemplatesTab.tsx
  - packages/frontend/src/pages/admin/notifications/NotificationPreferencesTab.tsx
  - packages/frontend/src/pages/admin/notifications/NotificationLogTab.tsx
  - packages/frontend/src/pages/admin/notifications/notificationHooks.ts
  - packages/frontend/src/pages/admin/notifications/notificationHelpers.ts
  - packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx
  - packages/frontend/src/pages/admin/dynamic-type/FieldOptionsEditor.tsx
  - packages/frontend/src/pages/admin/dynamic-type/ValidationRulesPanel.tsx
  - packages/frontend/src/pages/admin/dynamic-type/FieldConfigPanel.tsx
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "YardDashboard.tsx is under 400 lines with logic extracted into sub-component files in a yard/ subdirectory"
    - "NotificationRulesPage.tsx is under 400 lines with EmailTemplatesTab, NotificationPreferencesTab, and NotificationLogTab in a notifications/ subdirectory"
    - "DynamicTypeBuilderPage.tsx is under 400 lines with FieldOptionsEditor, ValidationRulesPanel, and FieldConfigPanel in a dynamic-type/ subdirectory"
    - "All three pages render identically after refactoring -- no visual or behavioral changes"
  artifacts:
    - path: "packages/frontend/src/pages/warehouse/YardDashboard.tsx"
      provides: "Slim orchestrator page under 400 LOC"
    - path: "packages/frontend/src/pages/warehouse/yard/ScheduleModal.tsx"
      provides: "Extracted schedule appointment modal"
    - path: "packages/frontend/src/pages/warehouse/yard/CheckInTruckModal.tsx"
      provides: "Extracted truck check-in modal"
    - path: "packages/frontend/src/pages/admin/notifications/EmailTemplatesTab.tsx"
      provides: "Extracted email templates tab (~400 LOC)"
    - path: "packages/frontend/src/pages/admin/notifications/NotificationPreferencesTab.tsx"
      provides: "Extracted notification preferences tab (~190 LOC)"
    - path: "packages/frontend/src/pages/admin/notifications/NotificationLogTab.tsx"
      provides: "Extracted notification log tab (~170 LOC)"
    - path: "packages/frontend/src/pages/admin/dynamic-type/FieldOptionsEditor.tsx"
      provides: "Extracted field options editor"
    - path: "packages/frontend/src/pages/admin/dynamic-type/ValidationRulesPanel.tsx"
      provides: "Extracted validation rules panel"
    - path: "packages/frontend/src/pages/admin/dynamic-type/FieldConfigPanel.tsx"
      provides: "Extracted field config panel"
  key_links:
    - from: "packages/frontend/src/pages/warehouse/YardDashboard.tsx"
      to: "packages/frontend/src/pages/warehouse/yard/*.tsx"
      via: "named imports of extracted sub-components"
      pattern: "import.*from.*\\./yard/"
    - from: "packages/frontend/src/pages/admin/NotificationRulesPage.tsx"
      to: "packages/frontend/src/pages/admin/notifications/*.tsx"
      via: "named imports of extracted tab components"
      pattern: "import.*from.*\\./notifications/"
    - from: "packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx"
      to: "packages/frontend/src/pages/admin/dynamic-type/*.tsx"
      via: "named imports of extracted panel components"
      pattern: "import.*from.*\\./dynamic-type/"
---

<objective>
Refactor three large frontend components (YardDashboard 1081 LOC, NotificationRulesPage 1074 LOC, DynamicTypeBuilderPage 1044 LOC) to under 400 lines each by extracting sub-components into sibling subdirectories.

Purpose: Reduce component size for maintainability, faster IDE parsing, and easier targeted testing. No visual or behavioral changes.
Output: Three slim orchestrator pages with extracted sub-components in co-located subdirectories.
</objective>

<execution_context>
@/Users/a.rahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.rahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the three large components. -->

YardDashboard.tsx (1081 LOC) internal structure:
```
Lines 1-32:    Imports (lucide-react, hooks, recharts, types)
Lines 33-55:   Status color constants (DOCK_STATUS_COLOR, APPT_STATUS_COLOR, TRUCK_STATUS_COLOR)
Lines 56-77:   Helper functions (formatTime, formatDateTime, getDuration)
Lines 79-96:   KpiCard sub-component (inline)
Lines 98-281:  ScheduleModal sub-component (inline, ~183 LOC)
Lines 282-409: CheckInTruckModal sub-component (inline, ~127 LOC)
Lines 410-1081: Main YardDashboard component (~671 LOC, contains dock grid, appointments table, trucks table, dock utilization chart)
```

NotificationRulesPage.tsx (1074 LOC) internal structure:
```
Lines 1-26:    Imports
Lines 27-69:   Type definitions (EmailTemplate, EmailLog, NotificationPreference, etc.)
Lines 71-96:   Constants (TABS, EVENT_TYPES, LOG_STATUSES, EMPTY_TEMPLATE)
Lines 106-175: Custom hooks (useEmailTemplates, useCreateEmailTemplate, etc.)
Lines 176-230: Helper functions (formatDate, getStatusColor, getStatusIcon, sanitizePreviewHtml)
Lines 231-280: Main NotificationRulesPage component (tab container, ~50 LOC)
Lines 282-682: EmailTemplatesTab (~400 LOC)
Lines 683-872: NotificationPreferencesTab (~190 LOC)
Lines 873-1074: NotificationLogTab (~200 LOC)
```

DynamicTypeBuilderPage.tsx (1044 LOC) internal structure:
```
Lines 1-25:    Imports and constants (COL_SPAN_MAP)
Lines 26-135:  FieldOptionsEditor sub-component (~110 LOC)
Lines 136-329: ValidationRulesPanel sub-component (~193 LOC)
Lines 330-461: FieldConfigPanel sub-component (~131 LOC)
Lines 462-484: FIELD_TYPES constant and TABS constant
Lines 486-1044: Main DynamicTypeBuilderPage component (~558 LOC)
```

Shared import conventions:
```typescript
// Path alias: '@' -> packages/frontend/src/
import { KpiCard } from '@/components/KpiCard';
import { useYardStatus } from '@/domains/warehouse-ops/hooks/useYard';
```

Component export convention:
```typescript
// Named exports for components
export const YardDashboard: React.FC = () => { ... };
export function NotificationRulesPage() { ... }
export const DynamicTypeBuilderPage: React.FC = () => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract YardDashboard sub-components into yard/ subdirectory</name>
  <read_first>
    - packages/frontend/src/pages/warehouse/YardDashboard.tsx (full file)
    - packages/frontend/src/domains/warehouse-ops/hooks/useYard.ts (type imports used by sub-components)
  </read_first>
  <files>
    packages/frontend/src/pages/warehouse/YardDashboard.tsx
    packages/frontend/src/pages/warehouse/yard/YardKpiCards.tsx
    packages/frontend/src/pages/warehouse/yard/ScheduleModal.tsx
    packages/frontend/src/pages/warehouse/yard/CheckInTruckModal.tsx
    packages/frontend/src/pages/warehouse/yard/YardDockGrid.tsx
    packages/frontend/src/pages/warehouse/yard/YardAppointmentsTable.tsx
    packages/frontend/src/pages/warehouse/yard/YardTrucksTable.tsx
    packages/frontend/src/pages/warehouse/yard/YardDockChart.tsx
  </files>
  <action>
    Create `packages/frontend/src/pages/warehouse/yard/` directory and extract sub-components:

    1. **YardKpiCards.tsx**: Extract the KpiCard component and the status color constants (DOCK_STATUS_COLOR, APPT_STATUS_COLOR, TRUCK_STATUS_COLOR). Export the constants too since they're used by multiple sub-components. Also export the helper functions (formatTime, formatDateTime, getDuration).

    2. **ScheduleModal.tsx**: Extract the ScheduleModal component (lines ~98-281). It uses useCreateAppointment hook and receives warehouseId, dockDoors, onClose as props. Keep the same prop types.

    3. **CheckInTruckModal.tsx**: Extract the CheckInTruckModal component (lines ~282-409). Uses useCheckInTruck hook, receives onClose prop.

    4. **YardDockGrid.tsx**: Extract the dock doors grid rendering section from the main component into a sub-component. It receives dockDoors array and useAssignDock/useCheckOutTruck hooks, renders the grid of dock status cards with assign/release actions.

    5. **YardAppointmentsTable.tsx**: Extract the appointments table section. Receives appointments array and check-in/complete/cancel mutation hooks.

    6. **YardTrucksTable.tsx**: Extract the trucks table section. Receives trucks array.

    7. **YardDockChart.tsx**: Extract the dock utilization bar chart section that uses Recharts (ResponsiveContainer, BarChart). Receives utilization data from useDockUtilization hook.

    8. **Slim YardDashboard.tsx**: Rewrite the main file to:
       - Import all sub-components from `./yard/`
       - Keep the useState for warehouse selection, modal visibility, and tab state
       - Keep the hook calls (useYardStatus, useWarehouses, useDockUtilization)
       - Compose sub-components in the render tree
       - Target: under 400 LOC (ideally ~250-350)

    All extracted components use named exports. Follow existing conventions: glass-card, Nesma tokens, lucide-react icons. Each extracted file includes its own import statements. Do NOT change any className, styling, or behavior -- pure structural extraction.

    Pass required mutation hooks or their results as props to sub-components rather than calling hooks inside extracted components (this keeps hook calls centralized in the parent). Exception: sub-components that are self-contained modals (ScheduleModal, CheckInTruckModal) can keep their own mutation hooks since they're logically independent.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - wc -l packages/frontend/src/pages/warehouse/YardDashboard.tsx returns under 400
    - ls packages/frontend/src/pages/warehouse/yard/ shows at least 4 extracted component files
    - grep -c "import.*from.*\\./yard/" packages/frontend/src/pages/warehouse/YardDashboard.tsx returns at least 4
    - npx tsc --noEmit in packages/frontend shows 0 errors
  </acceptance_criteria>
  <done>YardDashboard.tsx is under 400 LOC. Sub-components extracted to yard/ subdirectory. TypeScript compiles with zero errors. No visual or behavioral changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract NotificationRulesPage tabs and DynamicTypeBuilderPage panels into subdirectories</name>
  <read_first>
    - packages/frontend/src/pages/admin/NotificationRulesPage.tsx (full file)
    - packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx (full file)
  </read_first>
  <files>
    packages/frontend/src/pages/admin/NotificationRulesPage.tsx
    packages/frontend/src/pages/admin/notifications/EmailTemplatesTab.tsx
    packages/frontend/src/pages/admin/notifications/NotificationPreferencesTab.tsx
    packages/frontend/src/pages/admin/notifications/NotificationLogTab.tsx
    packages/frontend/src/pages/admin/notifications/notificationHooks.ts
    packages/frontend/src/pages/admin/notifications/notificationHelpers.ts
    packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx
    packages/frontend/src/pages/admin/dynamic-type/FieldOptionsEditor.tsx
    packages/frontend/src/pages/admin/dynamic-type/ValidationRulesPanel.tsx
    packages/frontend/src/pages/admin/dynamic-type/FieldConfigPanel.tsx
  </files>
  <action>
    **Part A -- NotificationRulesPage refactoring:**

    Create `packages/frontend/src/pages/admin/notifications/` directory:

    1. **notificationHooks.ts**: Extract the custom hooks (useEmailTemplates, useCreateEmailTemplate, useUpdateEmailTemplate, useDeleteEmailTemplate, usePreviewTemplate, useEmailLogs) from lines 106-175. These hooks use apiClient and React Query -- export them as named exports.

    2. **notificationHelpers.ts**: Extract the helper functions and constants (formatDate, getStatusColor, getStatusIcon, sanitizePreviewHtml, EVENT_TYPES, LOG_STATUSES, EMPTY_TEMPLATE) from lines 71-230. Also extract the type definitions (EmailTemplate, EmailLog, NotificationPreference, Tab type) so they can be shared across tabs.

    3. **EmailTemplatesTab.tsx**: Extract the EmailTemplatesTab function component (lines 282-682, ~400 LOC). Import hooks from notificationHooks.ts and helpers from notificationHelpers.ts. Keep it as a named export.

    4. **NotificationPreferencesTab.tsx**: Extract the NotificationPreferencesTab function component (lines 683-872, ~190 LOC). Import shared types and helpers.

    5. **NotificationLogTab.tsx**: Extract the NotificationLogTab function component (lines 873-1074, ~200 LOC). Import shared types and helpers.

    6. **Slim NotificationRulesPage.tsx**: Rewrite to:
       - Import the 3 tab components from `./notifications/`
       - Keep the TABS constant and tab switching state
       - Render the tab header and conditionally render tab content
       - Target: under 100 LOC (it's already a thin wrapper at lines 231-280)

    **Part B -- DynamicTypeBuilderPage refactoring:**

    Create `packages/frontend/src/pages/admin/dynamic-type/` directory:

    1. **FieldOptionsEditor.tsx**: Extract the FieldOptionsEditor component (lines 26-135, ~110 LOC). Keep the FieldOptionsEditorProps interface. Export INPUT_CLS constant since it's shared.

    2. **ValidationRulesPanel.tsx**: Extract the ValidationRulesPanel component (lines 136-329, ~193 LOC). Keep its props interface. Import INPUT_CLS from FieldOptionsEditor.

    3. **FieldConfigPanel.tsx**: Extract the FieldConfigPanel component (lines 330-461, ~131 LOC). It uses FieldOptionsEditor and ValidationRulesPanel internally -- import them from sibling files.

    4. **Slim DynamicTypeBuilderPage.tsx**: Rewrite to:
       - Import FieldConfigPanel from `./dynamic-type/`
       - Keep the FIELD_TYPES, TABS, COL_SPAN_MAP constants
       - Keep the main component logic (hooks, state, form handling)
       - Keep the render tree but use FieldConfigPanel for the field editing section
       - Target: under 400 LOC

    All extracted components use named exports. Follow existing conventions: glass-card, Nesma tokens, lucide-react icons, INPUT_CLS pattern. Do NOT change any className, styling, or behavior. Pure structural extraction.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - wc -l packages/frontend/src/pages/admin/NotificationRulesPage.tsx returns under 400
    - wc -l packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx returns under 400
    - ls packages/frontend/src/pages/admin/notifications/ shows at least 3 tab component files
    - ls packages/frontend/src/pages/admin/dynamic-type/ shows at least 3 panel component files
    - grep -c "import.*from.*\\./notifications/" packages/frontend/src/pages/admin/NotificationRulesPage.tsx returns at least 3
    - grep -c "import.*from.*\\./dynamic-type/" packages/frontend/src/pages/admin/DynamicTypeBuilderPage.tsx returns at least 1
    - npx tsc --noEmit in packages/frontend shows 0 errors
  </acceptance_criteria>
  <done>NotificationRulesPage.tsx and DynamicTypeBuilderPage.tsx are both under 400 LOC. Sub-components extracted to co-located subdirectories. TypeScript compiles with zero errors. No visual or behavioral changes.</done>
</task>

</tasks>

<verification>
1. Line counts: `wc -l` for all 3 parent files returns under 400 each
2. TypeScript: `cd packages/frontend && npx tsc --noEmit` -- 0 errors
3. Build: `cd packages/frontend && npx vite build` completes successfully
4. No import errors: All extracted sub-components are properly imported in their parent files
</verification>

<success_criteria>
- QUAL-01: YardDashboard.tsx, NotificationRulesPage.tsx, and DynamicTypeBuilderPage.tsx are each under 400 lines
- All extracted sub-components live in co-located subdirectories (yard/, notifications/, dynamic-type/)
- TypeScript compilation passes with zero errors
- Vite build succeeds
- No visual or behavioral changes (pure structural refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-quality/06-02-SUMMARY.md`
</output>
