---
phase: 07-end-to-end-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/domains/workflow/services/approval.service.test.ts
  - packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts
  - packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts
  - packages/backend/src/socket/setup.test.ts
autonomous: true
requirements:
  - VERF-02
  - VERF-06
  - VERF-07
must_haves:
  truths:
    - "A 3-level sequential approval chain advances through each level, with the document reaching 'approved' only after the final level approves"
    - "A parallel approval group in 'all' mode requires all approvers to approve before the group resolves"
    - "A parallel approval group in 'any' mode resolves on the first approve response"
    - "When one parallel approver is slow (hasn't responded), the group stays pending until quorum is met"
    - "MI submission triggers N-01 notification to the appropriate approver role"
    - "GRN submit with rfimRequired triggers N-06 QC inspection notification to qc_officer"
    - "Document status changes emit Socket.IO events to the correct role-based rooms"
    - "Socket.IO auth middleware rejects connections without a valid JWT"
  artifacts:
    - path: "packages/backend/src/domains/workflow/services/approval.service.test.ts"
      provides: "Sequential multi-level approval verification"
      contains: "3-level sequential"
    - path: "packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts"
      provides: "Parallel approval verification with all/any mode and slow approver"
      contains: "parallel approval lifecycle"
    - path: "packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts"
      provides: "Notification delivery tests for MI submit (N-01) and QCI required (N-06)"
      contains: "N-01"
    - path: "packages/backend/src/socket/setup.test.ts"
      provides: "Socket.IO authentication and event emission tests"
      contains: "setupSocketIO"
  key_links:
    - from: "approval.service.ts processApproval()"
      to: "notification.service.ts createNotification()"
      via: "notifyRoleUsers helper"
      pattern: "createNotification"
    - from: "notification-dispatcher.service.ts"
      to: "event-bus.ts eventBus.subscribe()"
      via: "event listener registration"
      pattern: "eventBus\\.subscribe"
    - from: "socket/setup.ts emitToRole()"
      to: "io.to(room).emit()"
      via: "Socket.IO room broadcast"
      pattern: "emitToRole"
---

<objective>
Verify that approval workflows (sequential and parallel), notification delivery, and Socket.IO real-time updates function correctly through their complete lifecycles.

Purpose: These three systems are tightly coupled -- approvals trigger notifications which trigger Socket.IO events. Testing them together verifies the event-driven chain works end-to-end. The parallel approval concurrency scenario (STATE.md blocker: "may surface race conditions") gets specific attention.

Output: 4 expanded/new test files covering multi-level sequential approval, parallel approval with all/any modes, notification dispatch for workflow triggers, and Socket.IO auth + event emission.
</objective>

<execution_context>
@/Users/a.rahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.rahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key source files:
@packages/backend/src/domains/workflow/services/approval.service.ts
@packages/backend/src/domains/workflow/services/parallel-approval.service.ts
@packages/backend/src/domains/notifications/services/notification-dispatcher.service.ts
@packages/backend/src/socket/setup.ts
@packages/backend/src/events/event-bus.ts
@packages/backend/src/test-utils/prisma-mock.ts
@packages/backend/src/test-utils/test-app.ts

<interfaces>
From packages/backend/src/domains/workflow/services/approval.service.ts:
- `getApprovalChain(documentType, amount?)` -> returns `{ steps: Array<{ level, approverRole, slaHours }> }`
- `getRequiredApproval(documentType, amount?)` -> returns first step from chain
- `submitForApproval(params: { documentType, documentId, submittedById, io?, totalValue? })` -> creates approval steps
- `processApproval(params: { documentType, documentId, action, processedById, comments?, io? })` -> processes approve/reject at current level
- `getApprovalSteps(documentType, documentId)` -> returns all steps for document
- `getPendingApprovalsForUser(userId)` -> returns pending approvals for user

From packages/backend/src/domains/workflow/services/parallel-approval.service.ts:
- `createParallelApproval(params: { documentType, documentId, level, mode: 'all'|'any', approverIds })` -> creates group
- `recordParallelResponse(params: { groupId, approverId, decision: 'approve'|'reject', comments? })` -> records response, resolves group if quorum met
- `getParallelApprovalGroup(groupId)` -> returns group with responses
- `getGroupsForDocument(documentType, documentId)` -> returns all groups

From packages/backend/src/domains/notifications/services/notification-dispatcher.service.ts:
- Subscribes to eventBus events
- N-01: MI submitted -> notifies approver role
- N-06: QC inspection required -> notifies qc_officer
- `notifyRecipients(params)` -> creates batch notifications
- `getRecipientsByRoles(roles)` -> fetches active employees by role

From packages/backend/src/socket/setup.ts:
- `setupSocketIO(io)` -> configures auth middleware, room management, event handlers
- `emitToRole(io, role, event, data)` -> broadcasts to role-based room
- `emitToUser(io, userId, event, data)` -> sends to specific user
- `emitToDocument(io, documentType, documentId, event, data)` -> sends to document subscribers
- DOC_TYPE_RESOURCE mapping: mrrv->grn, mirv->mi, mrv->mrn, etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sequential and parallel approval workflow tests</name>
  <files>
    packages/backend/src/domains/workflow/services/approval.service.test.ts,
    packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts
  </files>
  <read_first>
    packages/backend/src/domains/workflow/services/approval.service.ts,
    packages/backend/src/domains/workflow/services/approval.service.test.ts,
    packages/backend/src/domains/workflow/services/parallel-approval.service.ts,
    packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts,
    packages/backend/src/test-utils/prisma-mock.ts,
    packages/backend/src/events/event-bus.ts
  </read_first>
  <behavior>
    Sequential approval:
    - Test 1: submitForApproval creates 3 approval steps (levels 1, 2, 3) when chain has 3 levels
    - Test 2: processApproval at level 1 with action='approve' advances to level 2 (step 1 status='approved', step 2 status='pending')
    - Test 3: processApproval at level 2 with action='approve' advances to level 3
    - Test 4: processApproval at level 3 with action='approve' marks document as fully approved (no more pending steps)
    - Test 5: processApproval with action='reject' at any level rejects the document without advancing to next level
    - Test 6: processApproval calls createNotification for the next level's approverRole when advancing

    Parallel approval:
    - Test 7: createParallelApproval with mode='all' and 2 approverIds creates a pending group
    - Test 8: recordParallelResponse with first approver approve -> group stays pending (waiting for second)
    - Test 9: recordParallelResponse with second approver approve -> group resolves to 'approved'
    - Test 10: mode='all', one approve + one reject -> group resolves to 'rejected'
    - Test 11: mode='any', first approver approve -> group resolves to 'approved' immediately
    - Test 12: mode='any', first approver reject, second approve -> group resolves to 'approved'
    - Test 13: Slow approver scenario: mode='all' with 3 approvers, 2 approve, 1 has not responded -> group stays 'pending'
  </behavior>
  <action>
    **approval.service.test.ts** -- Add `describe('sequential multi-level approval', () => { ... })` block at the END of the existing file.

    For the 3-level sequential test:
    1. Mock `mockPrisma.approvalWorkflow.findMany` to return 3 workflows with levels 1, 2, 3 (approverRoles: 'warehouse_supervisor', 'manager', 'admin').
    2. For `submitForApproval`: mock `mockPrisma.approvalStep.createMany` and verify it's called with `data` array of 3 steps. Mock `mockPrisma.approvalStep.findMany` to return the created steps with statuses.
    3. For `processApproval` at each level: mock `mockPrisma.approvalStep.findMany` to return steps where current level is 'pending' and all previous are 'approved'. Mock `mockPrisma.approvalStep.update` and `mockPrisma.approvalStep.findFirst` for the "find next pending" logic.
    4. Assert `createNotification` is called after level 1 approve to notify the level 2 role.
    5. For reject: mock level 2 as 'pending', call processApproval with action='reject'. Assert status becomes 'rejected' and level 3 step is NOT advanced.

    Mock the document model lookup: `processApproval` calls `getPrismaDelegate(documentType)` then `findUnique` on the result. Mock `getPrismaDelegate` to return a Prisma model mock that returns a document with appropriate status. Also mock `createAuditLog`, `emitToRole`, `eventBus.publish`.

    **parallel-approval.service.test.ts** -- Add `describe('parallel approval lifecycle', () => { ... })` block at the END of the existing file.

    For parallel tests:
    1. Mock `mockPrisma.employee.findMany` to return active employees for given IDs.
    2. Mock `mockPrisma.parallelApprovalGroup.create` to return a group with `status: 'pending'`.
    3. For `recordParallelResponse`: mock `mockPrisma.parallelApprovalGroup.findUnique` to return the group with current responses. Mock `mockPrisma.parallelApprovalResponse.create` for the new response.
    4. For mode='all': after 1 of 2 approve, assert `mockPrisma.parallelApprovalGroup.update` is NOT called with `status: 'approved'`. After 2 of 2, assert it IS.
    5. For mode='any': after first approve, assert group is immediately resolved to 'approved'.
    6. For slow approver: 3 approvers, 2 approve, 1 missing. Assert group.status remains 'pending'. The `recordParallelResponse` function checks `responses.length === totalApprovers` for 'all' mode resolution.
  </action>
  <verify>
    <automated>cd /Users/a.rahman/Projects/V2 && pnpm --filter @nit-scs-v2/backend exec vitest run src/domains/workflow/services/approval.service.test.ts src/domains/workflow/services/parallel-approval.service.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sequential multi-level approval" packages/backend/src/domains/workflow/services/approval.service.test.ts returns 1
    - grep -c "level 3" packages/backend/src/domains/workflow/services/approval.service.test.ts returns at least 2
    - grep -c "parallel approval lifecycle" packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts returns 1
    - grep -c "slow approver\|stays pending" packages/backend/src/domains/workflow/services/parallel-approval.service.test.ts returns at least 1
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>3-level sequential approval test verifies step-by-step advancement through all levels. Parallel approval tests verify 'all' mode needs all approvers, 'any' mode resolves on first approve, and slow approver keeps group pending. Reject at any point terminates the chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notification dispatch and Socket.IO event emission tests</name>
  <files>
    packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts,
    packages/backend/src/socket/setup.test.ts
  </files>
  <read_first>
    packages/backend/src/domains/notifications/services/notification-dispatcher.service.ts,
    packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts,
    packages/backend/src/socket/setup.ts,
    packages/backend/src/events/event-bus.ts,
    packages/backend/src/test-utils/prisma-mock.ts
  </read_first>
  <behavior>
    Notification dispatch:
    - Test 1: N-01 handler -- when eventBus publishes 'document:status_changed' with entityType 'mirv' and payload.to='pending_approval', notification is created for approver role users
    - Test 2: N-06 handler -- when eventBus publishes 'document:status_changed' with entityType 'mrrv' and payload.qciRequired=true, notification is created for qc_officer role
    - Test 3: N-02 handler -- when eventBus publishes 'inventory:low_stock', notification is created for warehouse_supervisor role
    - Test 4: Duplicate suppression -- a second identical event within 1 hour does NOT create a duplicate notification
    - Test 5: notifyRecipients() creates batch notifications for all recipient IDs

    Socket.IO:
    - Test 6: setupSocketIO auth middleware rejects connection without token (calls next(Error))
    - Test 7: setupSocketIO auth middleware rejects connection with invalid/expired token
    - Test 8: emitToRole broadcasts event to correct room name (`role:{roleName}`)
    - Test 9: emitToDocument emits to document-specific room (`doc:{type}:{id}`)
    - Test 10: DOC_TYPE_RESOURCE mapping correctly translates V1 names (mrrv->grn, mirv->mi)
  </behavior>
  <action>
    **notification-dispatcher.service.test.ts** -- This file already exists with some tests. Add `describe('workflow notification triggers', () => { ... })` block at the END.

    The notification dispatcher subscribes to eventBus events in its module initialization. To test this:
    1. Read the source file to understand exactly which events trigger which notifications.
    2. Mock all dependencies: `prisma` (with createPrismaMock), `log`, `sendPushToRole`.
    3. For N-01 (MI submitted): The dispatcher listens for `document:status_changed` where `entityType === 'mirv'` and `payload.to === 'pending_approval'`. Mock `prisma.employee.findMany` to return users with role matching the expected approver role. Assert `prisma.notification.createMany` is called with data array containing each recipient.
    4. For N-06 (QCI required): The dispatcher listens for `document:status_changed` where `entityType === 'mrrv'` and `payload.qciRequired === true`. Mock employees with role 'qc_officer'. Assert notification created.
    5. For duplicate suppression: Mock `prisma.notification.findFirst` to return an existing notification (recent). Assert `prisma.notification.createMany` is NOT called.

    Note: Since the dispatcher subscribes on module load, you may need to use `vi.importActual` or dynamically import the module AFTER setting up mocks. Read the existing test file first to see how it handles this.

    **socket/setup.test.ts** -- Create this NEW test file.

    Test the exported functions by mocking Socket.IO server:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    vi.mock('../utils/jwt.js', () => ({
      verifyAccessToken: vi.fn(),
    }));
    vi.mock('../config/logger.js', () => ({
      log: vi.fn(),
      logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn(), debug: vi.fn() },
    }));
    vi.mock('../utils/prisma.js', () => ({
      prisma: new Proxy({}, { get: () => new Proxy({}, { get: () => vi.fn().mockResolvedValue(null) }) }),
    }));
    vi.mock('../utils/prisma-helpers.js', () => ({ getPrismaDelegate: vi.fn() }));
    ```

    For auth middleware test: Create a mock `io` object with `io.use(middlewareFn)`. Capture the middleware function, call it with a mock socket (with `socket.handshake.auth.token` set or missing) and a `next` callback. Assert next is called with Error for missing/invalid token.

    For emitToRole test: Create a mock `io` with `io.to(room).emit(event, data)`. Call `emitToRole(io, 'admin', 'grn:created', { id: 'grn-1' })`. Assert `io.to` was called with `'role:admin'` and `.emit` was called with correct event and data.

    For DOC_TYPE_RESOURCE mapping: This is a const object in setup.ts. Import it or test via the permission check logic in the connection handler. Alternatively, test it indirectly via the `emitToDocument` function.
  </action>
  <verify>
    <automated>cd /Users/a.rahman/Projects/V2 && pnpm --filter @nit-scs-v2/backend exec vitest run src/domains/notifications/services/notification-dispatcher.service.test.ts src/socket/setup.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "workflow notification triggers" packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts returns 1
    - grep -c "N-01\|MI submitted" packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts returns at least 1
    - grep -c "N-06\|QC inspection" packages/backend/src/domains/notifications/services/notification-dispatcher.service.test.ts returns at least 1
    - test -f packages/backend/src/socket/setup.test.ts (file exists)
    - grep -c "setupSocketIO\|emitToRole" packages/backend/src/socket/setup.test.ts returns at least 2
    - grep -c "role:admin\|role:" packages/backend/src/socket/setup.test.ts returns at least 1
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>Notification dispatcher tests verify N-01 (MI submit) and N-06 (QCI required) create notifications for correct roles. Duplicate suppression test proves no double-notification within 1 hour. Socket.IO tests verify auth rejection for invalid tokens and correct room-based event emission.</done>
</task>

</tasks>

<verification>
Run both test files plus the approval tests:
```bash
cd /Users/a.rahman/Projects/V2 && pnpm --filter @nit-scs-v2/backend exec vitest run src/domains/workflow/ src/domains/notifications/ src/socket/ --reporter=verbose 2>&1 | tail -30
```
</verification>

<success_criteria>
- Sequential 3-level approval chain test passes through all levels, verifying step advancement
- Parallel approval tests cover 'all' mode, 'any' mode, and slow-approver scenario
- Notification N-01 and N-06 tests verify correct recipient roles
- Socket.IO auth test verifies rejected connections without valid JWT
- Socket.IO emitToRole test verifies correct room broadcast pattern
- All existing tests in these files continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-end-to-end-verification/07-02-SUMMARY.md`
</output>
