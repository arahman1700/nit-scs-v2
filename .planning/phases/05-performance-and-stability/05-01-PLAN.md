---
phase: 05-performance-and-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/prisma/schema/00-generators.prisma
  - packages/backend/prisma/schema/10-email-dashboard.prisma
  - packages/backend/prisma/schema/08-system.prisma
  - packages/backend/prisma/schema/06-inventory.prisma
  - packages/backend/prisma/schema/05-job-orders.prisma
autonomous: true
requirements: [PERF-02, PERF-04, PERF-05, PERF-06]

must_haves:
  truths:
    - "ApprovalStep queries by (documentType, documentId, status) use an index scan, not a sequential scan"
    - "AuditLog queries by (tableName, recordId, createdAt) use an index scan"
    - "Notification queries by (recipientId, isRead, createdAt) use an index scan"
    - "JobOrder queries by entityId use an index scan"
    - "InventoryLevel queries by (itemId, lastMovementDate) use an index scan"
    - "Prisma uses lateral JOINs for relation loading instead of separate queries"
    - "No route shadowing warnings appear on server startup"
    - "Frontend vendor chunks are correctly split for browser caching"
  artifacts:
    - path: "packages/backend/prisma/schema/10-email-dashboard.prisma"
      provides: "ApprovalStep composite index on (documentType, documentId, status)"
      contains: "@@index([documentType, documentId, status]"
    - path: "packages/backend/prisma/schema/08-system.prisma"
      provides: "AuditLog composite index on (tableName, recordId, createdAt) and Notification composite index on (recipientId, isRead, createdAt)"
      contains: "@@index"
    - path: "packages/backend/prisma/schema/06-inventory.prisma"
      provides: "InventoryLevel composite index on (itemId, lastMovementDate)"
      contains: "@@index([itemId, lastMovementDate]"
    - path: "packages/backend/prisma/schema/05-job-orders.prisma"
      provides: "JobOrder index on entityId"
      contains: "@@index([entityId]"
    - path: "packages/backend/prisma/schema/00-generators.prisma"
      provides: "relationJoins preview feature enabled"
      contains: "relationJoins"
  key_links:
    - from: "packages/backend/prisma/schema/*.prisma"
      to: "PostgreSQL indexes"
      via: "prisma db push or prisma migrate"
      pattern: "@@index"
    - from: "packages/backend/prisma/schema/00-generators.prisma"
      to: "Prisma query engine"
      via: "previewFeatures"
      pattern: "relationJoins"
---

<objective>
Add missing composite database indexes for high-frequency query patterns, enable Prisma relationJoins preview feature, and verify that route shadowing and vendor chunk splitting (completed in Phase 4) are working correctly.

Purpose: Database queries on ApprovalStep, AuditLog, Notification, JobOrder, and InventoryLevel currently hit sequential scans on composite filter patterns. Adding targeted indexes ensures these queries scale under realistic data volumes. The relationJoins feature switches Prisma from N separate SELECT queries for relations to efficient lateral JOINs.

Output: Updated Prisma schema files with new @@index declarations, generator with previewFeatures, and verification evidence for PERF-04 and PERF-05.
</objective>

<execution_context>
@/Users/a.rahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.rahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/backend/prisma/schema/00-generators.prisma
@packages/backend/prisma/schema/05-job-orders.prisma
@packages/backend/prisma/schema/06-inventory.prisma
@packages/backend/prisma/schema/08-system.prisma
@packages/backend/prisma/schema/10-email-dashboard.prisma
@packages/backend/src/utils/route-registry.ts
@packages/frontend/vite.config.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/backend/prisma/schema/10-email-dashboard.prisma (ApprovalStep):
Current indexes:
  @@unique([documentType, documentId, level], map: "uq_approval_step_doc_level")
  @@index([documentType, documentId], map: "idx_approval_steps_doc")
  @@index([approverId], map: "idx_approval_steps_approver")
  @@index([status], map: "idx_approval_steps_status")
Missing: composite (documentType, documentId, status) — queried in every approval lookup

From packages/backend/prisma/schema/08-system.prisma (AuditLog):
Current indexes:
  @@index([tableName, recordId], map: "idx_audit_log_table")
  @@index([performedAt(sort: Desc)], map: "idx_audit_log_date")
  @@index([performedById], map: "idx_audit_log_user")
Missing: composite (tableName, recordId, createdAt) for time-ordered audit trail queries

From packages/backend/prisma/schema/08-system.prisma (Notification):
Current indexes:
  @@index([recipientId, isRead], map: "idx_notifications_recipient")
  @@index([createdAt(sort: Desc)], map: "idx_notifications_date")
  @@index([referenceTable, referenceId], map: "idx_notifications_reference")
Missing: composite (recipientId, isRead, createdAt) for unread-notification-count queries

From packages/backend/prisma/schema/06-inventory.prisma (InventoryLevel):
Current indexes:
  @@unique([itemId, warehouseId], map: "uq_inventory_item_wh")
  @@index([warehouseId, itemId, qtyOnHand], map: "idx_mtl_onhand_avail_lookup")
  @@index([itemId, alertSent], map: "idx_mtl_onhand_reorder_alert")
Missing: composite (itemId, lastMovementDate) for dashboard movement queries

From packages/backend/prisma/schema/05-job-orders.prisma (JobOrder):
Current indexes include entityId implicitly via FK but NO explicit index
Missing: @@index([entityId]) for entity lookup queries

From packages/backend/prisma/schema/00-generators.prisma:
generator client {
  provider = "prisma-client-js"
}
Missing: previewFeatures = ["relationJoins"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composite database indexes and enable relationJoins</name>
  <read_first>
    packages/backend/prisma/schema/00-generators.prisma
    packages/backend/prisma/schema/10-email-dashboard.prisma
    packages/backend/prisma/schema/08-system.prisma
    packages/backend/prisma/schema/06-inventory.prisma
    packages/backend/prisma/schema/05-job-orders.prisma
  </read_first>
  <files>
    packages/backend/prisma/schema/00-generators.prisma
    packages/backend/prisma/schema/10-email-dashboard.prisma
    packages/backend/prisma/schema/08-system.prisma
    packages/backend/prisma/schema/06-inventory.prisma
    packages/backend/prisma/schema/05-job-orders.prisma
  </files>
  <action>
    1. In `00-generators.prisma`, add `previewFeatures = ["relationJoins"]` to the generator client block. Keep the existing provider line. The result should be:
       ```
       generator client {
         provider        = "prisma-client-js"
         previewFeatures = ["relationJoins"]
       }
       ```

    2. In `10-email-dashboard.prisma`, add a composite index to the ApprovalStep model. Place it after the existing @@index lines, before @@map:
       ```
       @@index([documentType, documentId, status], map: "idx_approval_steps_doc_status")
       ```
       This covers the WHERE clause pattern used in every approval lookup: `{ documentType, documentId, status: 'pending' }`.

    3. In `08-system.prisma`, add TWO indexes:
       a. AuditLog model — add after existing @@index lines:
          ```
          @@index([tableName, recordId, createdAt(sort: Desc)], map: "idx_audit_log_table_record_time")
          ```
          This covers time-ordered audit trail queries: `WHERE tableName = X AND recordId = Y ORDER BY createdAt DESC`.

       b. Notification model — add after existing @@index lines:
          ```
          @@index([recipientId, isRead, createdAt(sort: Desc)], map: "idx_notifications_unread_recent")
          ```
          This covers unread notification count and listing: `WHERE recipientId = X AND isRead = false ORDER BY createdAt DESC`.

    4. In `06-inventory.prisma`, add an index to InventoryLevel model after existing @@index lines:
       ```
       @@index([itemId, lastMovementDate(sort: Desc)], map: "idx_mtl_onhand_movement_date")
       ```
       This covers dashboard queries that filter/sort by last movement date.

    5. In `05-job-orders.prisma`, add an index to JobOrder model after existing @@index lines:
       ```
       @@index([entityId], map: "idx_job_orders_entity")
       ```
       This covers entity lookup queries (JO by entity relationship).

    6. Run `cd packages/backend && npx prisma generate` to regenerate the Prisma client with relationJoins enabled and the new index definitions.

    7. Run `cd packages/backend && npx prisma db push --accept-data-loss=false` or `npx prisma migrate dev --name add_performance_indexes` to apply the indexes. Prefer `prisma db push` if the migration history is not clean, but `prisma migrate dev` if you want a recorded migration. Use the timestamp naming convention established in Phase 4: `YYYYMMDDHHMMSS_name`.

    Do NOT remove or modify any existing indexes — only ADD new ones.
    Do NOT change the datasource block.
    Do NOT modify any model fields — only @@index lines.
  </action>
  <verify>
    <automated>cd /Users/a.rahman/Projects/V2/packages/backend && npx prisma validate && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'relationJoins' packages/backend/prisma/schema/00-generators.prisma
    - grep -q 'idx_approval_steps_doc_status' packages/backend/prisma/schema/10-email-dashboard.prisma
    - grep -q 'idx_audit_log_table_record_time' packages/backend/prisma/schema/08-system.prisma
    - grep -q 'idx_notifications_unread_recent' packages/backend/prisma/schema/08-system.prisma
    - grep -q 'idx_mtl_onhand_movement_date' packages/backend/prisma/schema/06-inventory.prisma
    - grep -q 'idx_job_orders_entity' packages/backend/prisma/schema/05-job-orders.prisma
    - npx prisma validate exits with code 0
  </acceptance_criteria>
  <done>
    Six new composite indexes added across 4 schema files. relationJoins preview feature enabled. Prisma client regenerated successfully. All existing indexes preserved unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify PERF-04 (vendor chunks) and PERF-05 (route shadowing) are resolved</name>
  <read_first>
    packages/frontend/vite.config.ts
    packages/backend/src/routes/index.ts
    packages/backend/src/utils/route-registry.ts
  </read_first>
  <files>
    packages/frontend/vite.config.ts
    packages/backend/src/routes/index.ts
  </files>
  <action>
    This task is VERIFICATION ONLY — confirm that Phase 4 work resolved PERF-04 and PERF-05.

    **PERF-04 (Vendor chunks):**
    1. Read `packages/frontend/vite.config.ts` and confirm `build.rollupOptions.output.manualChunks` contains at minimum:
       - `vendor-react`: react, react-dom, react-router-dom
       - `vendor-data`: @tanstack/react-query, axios, zustand
       - `vendor-forms`: react-hook-form, @hookform/resolvers, zod
       - `vendor-charts`: recharts
    2. Run `cd packages/frontend && npx vite build --mode production 2>&1 | tail -30` and confirm the output shows separate vendor chunk files (vendor-react-*.js, vendor-data-*.js, etc.).
    3. If the build fails, diagnose and fix. If vendor chunks are not splitting, that is a problem to fix. But based on Phase 4 Summary, this should already work.

    **PERF-05 (Route shadowing):**
    1. Read `packages/backend/src/routes/index.ts` and confirm the RouteRegistry is being used (not manual route ordering).
    2. Read `packages/backend/src/utils/route-registry.ts` and confirm it:
       - Introspects routes before mounting
       - Sorts domains so static routes mount before parameterized routes
       - Logs diagnostics about any remaining conflicts
    3. Run the backend test suite to confirm no route shadowing warnings: `cd packages/backend && npx tsx src/routes/index.ts 2>&1 | head -20` OR check test output for "[RouteRegistry]" messages.
    4. If there ARE still shadowing warnings, they need to be investigated. But based on the RouteRegistry implementation (which sorts by param density and resolves conflicts by mount order), warnings should now say "resolved by mount ordering" not "route shadowing detected".

    **If both are verified working:** No file changes needed. Document evidence in the summary.
    **If issues found:** Fix them. For PERF-04, adjust manualChunks. For PERF-05, check if RouteRegistry has unresolvable conflicts and add explicit prefixes if needed.
  </action>
  <verify>
    <automated>cd /Users/a.rahman/Projects/V2/packages/frontend && npx vite build --mode production 2>&1 | grep -c "vendor-" || echo "0 vendor chunks"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'manualChunks' packages/frontend/vite.config.ts
    - grep -q 'vendor-react' packages/frontend/vite.config.ts
    - grep -q 'vendor-data' packages/frontend/vite.config.ts
    - grep -q 'RouteRegistry' packages/backend/src/routes/index.ts
    - Frontend build produces separate vendor chunk files
  </acceptance_criteria>
  <done>
    PERF-04 confirmed: vendor chunk splitting produces at least 4 separate cached chunk files (react, data, forms, charts). PERF-05 confirmed: RouteRegistry automatically orders static routes before parameterized routes, eliminating shadowing conflicts.
  </done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes (schema is valid with new indexes and previewFeatures)
2. `npx prisma generate` succeeds (client regenerated with relationJoins)
3. Frontend build produces separate vendor-*.js chunk files
4. RouteRegistry is used for route mounting (no manual ordering)
5. All 6 new indexes are present in schema files
</verification>

<success_criteria>
- 6 composite indexes added to Prisma schema covering the 5 high-frequency query patterns identified in CONCERNS.md
- relationJoins preview feature enabled in Prisma generator
- PERF-04 (vendor chunks) verified as working from Phase 4
- PERF-05 (route shadowing) verified as resolved by RouteRegistry
- Prisma client regenerated successfully
- No existing indexes removed or modified
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-stability/05-01-SUMMARY.md`
</output>
